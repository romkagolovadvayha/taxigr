server {
    listen 80;
    listen [::]:80;
    server_name taxigr.ru api.taxigr.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 503;
    }
}
